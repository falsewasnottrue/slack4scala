# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /test       controllers.SlackTestController.test
GET     /cred       controllers.SlackTestController.cred